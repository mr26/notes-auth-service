apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-gateway.fullname" . }}-preview
spec:
  type: ClusterIP
  selector:
    app: {{ include "auth-gateway.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http

